# Base URL of the NestJS API (no trailing path — routes are at the root).
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:4000
# Production example:
# NEXT_PUBLIC_API_URL=https://d1-report-api.onrender.com
